// userRoutes removed — login/register endpoints removed per project change
// This file is intentionally left minimal to indicate removal.
import express from "express";
const userRouter = express.Router();
export default userRouter;
